from OneZero_One_Eight.one_one.data_structures import DataStructures
from OneZero_One_Eight.one_two.xml_parsing import XMLParsing
if input("1.1 Compare data formats (XML, JSON, and YAML)(Y/N)?") == "Y":
    DataStructures()

if input("1.2 Describe parsing of common data format (XML, JSON, and YAML) to Python data structures -XML (Y/N)?") == "Y":
    XMLParsing()
